(function(){
 function safeJournal(value=''){return String(value).replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]))}
 function localDateKey(value){const date=new Date(value);if(isNaN(date))return String(value||'').slice(0,10);return `${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,'0')}-${String(date.getDate()).padStart(2,'0')}`}
 function recordTone(status=''){if(/awaiting|pending/i.test(status))return'review';if(/urgent|immediate|alerted/i.test(status))return'alert';if(/recorded|confirmed|good|complete|reported/i.test(status))return'complete';return'neutral'}
 function recordArtwork(record){
  if(record.kind==='eggs'){
   const source=window.CCEMSArtwork.houses[Number(record.artwork)||0]||window.CCEMSArtwork.houses[0];
   return `<i class="record-icon collection-artwork explicit-record-artwork art-${Number(record.artwork)||0}" aria-hidden="true"><img src="${source}" alt=""></i>`;
  }
  if(record.kind==='livestock')return `<i class="record-icon record-livestock explicit-record-artwork" aria-hidden="true"><img src="${window.CCEMSArtwork.cattle}" alt=""></i>`;
  return '<i class="record-icon" aria-hidden="true"></i>';
 }
 journalV47=function(){const all=journalRecords(),rows=journalFilter==='all'?all:all.filter(record=>record.kind===journalFilter),now=new Date(),today=`${now.getFullYear()}-${String(now.getMonth()+1).padStart(2,'0')}-${String(now.getDate()).padStart(2,'0')}`,todayCount=all.filter(record=>localDateKey(record.createdAt)===today).length,employee=state().deviceRegistration?.employee||'Dean',filters=[['all','All'],['eggs','Eggs'],['safety','Safety'],['livestock','Welfare'],['concern','Concerns']],counts=Object.fromEntries(filters.map(([key])=>[key,key==='all'?all.length:all.filter(record=>record.kind===key).length])),emptyLabels={all:'submitted',eggs:'egg collection',safety:'safety',livestock:'welfare',concern:'concern'},formatDate=value=>{if(!value)return'Date not recorded';const date=new Date(value);return isNaN(date)?safeJournal(value):new Intl.DateTimeFormat('en-GB',{day:'numeric',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit'}).format(date)},kicker=record=>record.kind==='eggs'?'HERITAGE EGGS':record.kind==='safety'?'SAFETY & COMPLIANCE':record.kind==='livestock'?'ANIMAL WELFARE':'ESTATE CONCERN';return `<section class="journal-page">${journalHead('My Field Journal')}<main class="paper-workflow field-journal-v47 field-journal-v92"><div class="journal-v47-heading"><div class="journal-book-seal" aria-hidden="true"><i></i></div><small>ESTATE RECORDS</small><h2>Submitted Records</h2><div class="pretask-divider" aria-hidden="true"><span></span></div><p>A secure record of work submitted from this approved estate device.</p></div><section class="journal-summary"><article><strong>${todayCount}</strong><span>Submitted today</span></article><article><strong>${all.length}</strong><span>Total records</span></article><article><strong>${safeJournal(employee)}</strong><span>Approved user</span></article></section><nav class="journal-filters journal-filters-v92" aria-label="Filter submitted records">${filters.map(([key,label])=>`<button class="${journalFilter===key?'active':''}" aria-pressed="${journalFilter===key}" onclick="setJournalFilter('${key}')"><span>${label}</span><small>${counts[key]}</small></button>`).join('')}</nav><section class="journal-v47-records">${rows.length?rows.map(record=>`<button class="record-card record-${record.kind} record-tone-${recordTone(record.status)}" onclick="openJournalRecord(${all.indexOf(record)})">${recordArtwork(record)}<div><small>${kicker(record)}</small><h3>${safeJournal(record.title)}</h3><p>${safeJournal(record.amount)}</p><footer><span>${formatDate(record.createdAt)}</span><b>${safeJournal(record.status)}</b></footer><em>${safeJournal(record.reference||'Reference pending')} · ${safeJournal(record.worker)}${record.photo?' · Photograph attached':''}</em></div><span class="record-chevron" aria-hidden="true">›</span></button>`).join(''):`<div class="journal-empty"><i></i><h3>No ${emptyLabels[journalFilter]||'submitted'} records yet</h3><p>Completed records of this type will appear here automatically.</p></div>`}</section><p class="journal-device-note"><strong>Approved-device record</strong><span>Records shown here are retained on this device for authorised estate review.</span></p><button class="journal-secondary journal-home-button" onclick="go('home')">Back to My Day</button></main></section>`};
})();
